# Answer sheet: Product-led growth

Files for the lecture **Product-led growth: loops, mechanics, tests** (`../index.html`).

- `index.html` – the loop numbers, a worked mechanic for Fika & Co, every Tag Manager setting, and the A/B test answers.
- `growth-loop.csv` – the sheet from Part 1 with formulas. Import into Google Sheets (File → Import) and the formulas work.
- `ab-test.csv` – the sheet from Part 4 with formulas.
- `experiment-tag.html` – the Custom HTML tag from step 11, with a copy button.

Sheets with a Swedish locale want `;` instead of `,` inside formulas. The CSV files use `,`; change the locale under File → Settings if the formulas show as text.

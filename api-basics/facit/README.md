# Answer sheet: APIs

Files for the lecture **APIs: request, key, response** (`../index.html`).

- `index.html` – every command from the slides with the expected answer, and the numbers from step 15.
- `kaggle.js` – the script from step 17. Replace the token, then `node kaggle.js`.
- `kaggle-campaigns.js` – the script after step 19, with one line per campaign.

Both scripts need Node 18 or later. No `npm install` is needed: `fetch` is built in.
